# rexgraph/viz - visualization
py.install_sources(
    ['__init__.py', 'dashboard.py'],
    subdir: 'rexgraph/viz',
)
# Templates are installed as package data
install_subdir('templates', install_dir: py.get_install_dir() / 'rexgraph/viz')
